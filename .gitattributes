# 文本/二进制自动判定
* text=auto

# Shell 脚本必须 LF —— bash 不接受 CRLF 行尾，否则 mac/linux 上报 $'\r': command not found，hook 全失效
*.sh  text eol=lf

# PowerShell 脚本用 CRLF —— Windows 原生（task-trace 为 Windows 优先）
*.ps1 text eol=crlf

*.md   text
*.json text
